{
  "name": "leapfrog",
  "version": "0.0.0",
  "private": true,
  "description": "Competitive-intelligence platform for the software-supply-chain / artifact-management market.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@11.17.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc --build && npm run typecheck --workspace @leapfrog/web",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev": "npm run dev --workspace @leapfrog/web",
    "seed": "npm run build --workspace @leapfrog/core && npm run seed --workspace @leapfrog/worker --",
    "fetch": "npm run build --workspace @leapfrog/core && npm run fetch --workspace @leapfrog/worker --",
    "ingest": "npm run build --workspace @leapfrog/core && npm run ingest --workspace @leapfrog/worker --",
    "enrich": "npm run build --workspace @leapfrog/core && npm run enrich --workspace @leapfrog/worker --",
    "embed": "npm run build --workspace @leapfrog/core && npm run embed --workspace @leapfrog/worker --",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/node": "^22.7.0",
    "eslint": "^9.13.0",
    "eslint-config-prettier": "^9.1.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.11.0",
    "vitest": "^2.1.3"
  }
}

import { APP_NAME } from '@leapfrog/core';

/**
 * Web app placeholder. The Next.js (App Router) UI shell — sidebar, theme, and
 * routing — lands in `feat/ui-shell` (M1, issue #4). Kept as a typed stub so the
 * workspace resolves and CI stays green from commit #1.
 */
export const appName = APP_NAME;
